<UserControl x:Class="ClubPay.Agent.Client.Views.LockScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cv="clr-namespace:ClubPay.Agent.Client.Converters"
             Background="{StaticResource BgGradient}">
  <UserControl.Resources>
    <cv:BoolToVisibilityConverter x:Key="BoolVis"/>
  </UserControl.Resources>

  <Viewbox Stretch="Uniform">
    <Grid Width="1920" Height="1080">
      <!-- Normal payment screen. It is fully removed while this PC is reserved. -->
      <Grid>
        <Grid.Style>
          <Style TargetType="Grid">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsReserved}" Value="True">
                <Setter Property="Visibility" Value="Collapsed"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </Grid.Style>
        <Rectangle Opacity="0.018">
          <Rectangle.Fill>
            <DrawingBrush TileMode="Tile" Viewport="0,0,47,47" ViewportUnits="Absolute">
              <DrawingBrush.Drawing>
                <DrawingGroup>
                  <GeometryDrawing>
                    <GeometryDrawing.Pen><Pen Brush="White" Thickness="1"/></GeometryDrawing.Pen>
                    <GeometryDrawing.Geometry><GeometryGroup><LineGeometry StartPoint="47,0" EndPoint="47,47"/><LineGeometry StartPoint="0,47" EndPoint="47,47"/></GeometryGroup></GeometryDrawing.Geometry>
                  </GeometryDrawing>
                </DrawingGroup>
              </DrawingBrush.Drawing>
            </DrawingBrush>
          </Rectangle.Fill>
        </Rectangle>
        <Rectangle Fill="{StaticResource Vignette}"/>
        <Grid Margin="64,46,64,0" VerticalAlignment="Top" Height="62">
          <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <TextBlock Text="{Binding ClubName}" Foreground="White" FontFamily="{StaticResource FontChakra}" FontSize="30" FontWeight="Bold"/>
            <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[GamingClub], Mode=OneWay}" Foreground="{StaticResource CyanBrush}" FontFamily="{StaticResource FontMono}" FontSize="15"/>
          </StackPanel>
          <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
            <ComboBox Style="{StaticResource LanguagePicker}" Margin="0,0,18,0" SelectedValue="{Binding Source={StaticResource Loc}, Path=LanguageCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag"><ComboBoxItem Content="RU" Tag="ru"/><ComboBoxItem Content="UZ" Tag="uz"/></ComboBox>
            <TextBlock Text="{Binding CurrentTime}" Foreground="{StaticResource TextSecondary}" FontFamily="{StaticResource FontMono}" FontSize="26" Margin="0,0,18,0"/>
            <Border CornerRadius="8" BorderThickness="1" Padding="16,9" Background="#19FF3B5C" BorderBrush="#66FF3B5C"><StackPanel Orientation="Horizontal"><Ellipse Width="10" Height="10" Fill="{StaticResource RedBrush}" Margin="0,0,10,0"/><TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Locked], Mode=OneWay}" Foreground="{StaticResource RedSoftBrush}" FontFamily="{StaticResource FontChakra}" FontSize="18" FontWeight="SemiBold"/></StackPanel></Border>
          </StackPanel>
        </Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,40,0,0">
          <Border CornerRadius="8" BorderThickness="1" Padding="20,8" Background="#14FFFFFF" BorderBrush="#5900D4FF" HorizontalAlignment="Center" Margin="0,0,0,14"><TextBlock Text="{Binding ZoneLabel}" Foreground="#FFCFE9F5" FontFamily="{StaticResource FontChakra}" FontSize="22" FontWeight="Medium"/></Border>
          <TextBlock Text="{Binding PcId}" HorizontalAlignment="Center" Foreground="White" FontFamily="{StaticResource FontChakra}" FontSize="100" FontWeight="Bold"/>
          <StackPanel Margin="0,38,0,0"><Border CornerRadius="20" Background="White" Padding="22" HorizontalAlignment="Center"><Image Source="{Binding PayQrImage}" Width="300" Height="300" RenderOptions.BitmapScalingMode="NearestNeighbor"/></Border><TextBlock Text="{Binding Source={StaticResource Loc}, Path=[ScanToPay], Mode=OneWay}" HorizontalAlignment="Center" Margin="0,18,0,0" Foreground="{StaticResource GreenBrush}" FontFamily="{StaticResource FontChakra}" FontSize="26" FontWeight="SemiBold"/></StackPanel>
        </StackPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="64,0,0,44"><Border CornerRadius="12" Background="White" Padding="10"><Image Source="{Binding WifiQrImage}" Width="108" Height="108" RenderOptions.BitmapScalingMode="NearestNeighbor"/></Border><StackPanel Margin="16,0,0,0" VerticalAlignment="Center" MaxWidth="320"><TextBlock Text="{Binding Source={StaticResource Loc}, Path=[NoInternet], Mode=OneWay}" Foreground="{StaticResource TextSecondary}" FontFamily="{StaticResource FontChakra}" FontSize="18" FontWeight="SemiBold"/><TextBlock Text="{Binding Source={StaticResource Loc}, Path=[WifiHelp], Mode=OneWay}" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" FontFamily="{StaticResource FontMono}" FontSize="14" Margin="0,3,0,0"/></StackPanel></StackPanel>
      </Grid>

      <!-- The reserved state intentionally has no payment QR, clock, status badge or Wi-Fi QR. -->
      <Grid Background="#FFF3D782">
        <Grid.Style>
          <Style TargetType="Grid">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers><DataTrigger Binding="{Binding IsReserved}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
          </Style>
        </Grid.Style>
        <StackPanel Margin="72,62,72,0" VerticalAlignment="Top">
          <TextBlock Text="{Binding ClubName}" Foreground="#FF332A16" FontFamily="{StaticResource FontChakra}" FontSize="34" FontWeight="Bold"/>
          <TextBlock Text="{Binding PcId}" Foreground="#FF6B5A2A" FontFamily="{StaticResource FontMono}" FontSize="22" Margin="0,7,0,0"/>
        </StackPanel>
        <StackPanel Width="680" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-20,0,0">
          <TextBlock Text="ПК ЗАБРОНИРОВАН" HorizontalAlignment="Center" Foreground="#FF332A16" FontFamily="{StaticResource FontChakra}" FontSize="48" FontWeight="Bold"/>
          <TextBlock Text="Ваша бронь начинается в" HorizontalAlignment="Center" Margin="0,28,0,0" Foreground="#FF6B5A2A" FontFamily="{StaticResource FontChakra}" FontSize="24"/>
          <TextBlock Text="{Binding ReservationStart}" HorizontalAlignment="Center" Foreground="#FF332A16" FontFamily="{StaticResource FontMono}" FontSize="72" FontWeight="Bold"/>
          <StackPanel Margin="0,38,0,0">
            <StackPanel.Style><Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding CanEnterReservationCode}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></StackPanel.Style>
            <TextBlock Text="Введите код из приложения ClubPay" HorizontalAlignment="Center" Foreground="#FF332A16" FontFamily="{StaticResource FontChakra}" FontSize="23"/>
            <TextBox Text="{Binding ReservationEntry, UpdateSourceTrigger=PropertyChanged}" MaxLength="6" Width="370" Height="72" Margin="0,18,0,0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontFamily="{StaticResource FontMono}" FontSize="36" FontWeight="Bold" Foreground="#FF332A16" Background="#FFFFFAE9" BorderBrush="#FF9E812D" BorderThickness="2"/>
            <Button Command="{Binding CheckInReservationCommand}" IsEnabled="{Binding CanSubmitReservationCode}" Width="370" Height="64" Margin="0,16,0,0" FontFamily="{StaticResource FontChakra}" FontSize="24" FontWeight="SemiBold" Foreground="White" Background="#FF5D4912" BorderThickness="0" Content="Начать игру"/>
          </StackPanel>
          <TextBlock Text="{Binding ReservationWaitText}" HorizontalAlignment="Center" Margin="0,34,0,0" TextAlignment="Center" TextWrapping="Wrap" Foreground="#FF6B5A2A" FontFamily="{StaticResource FontChakra}" FontSize="21"/>
          <TextBlock Text="{Binding ReservationError}" HorizontalAlignment="Center" Margin="0,16,0,0" TextAlignment="Center" TextWrapping="Wrap" Foreground="#FF8B1E3F" FontFamily="{StaticResource FontChakra}" FontSize="20"/>
          <TextBlock Text="Если не начать игру в течение 15 минут после начала, бронь отменится и ПК снова станет свободным." HorizontalAlignment="Center" Margin="0,30,0,0" TextAlignment="Center" TextWrapping="Wrap" Foreground="#FF6B5A2A" FontFamily="{StaticResource FontChakra}" FontSize="18"/>
        </StackPanel>
      </Grid>
    </Grid>
  </Viewbox>
</UserControl>
